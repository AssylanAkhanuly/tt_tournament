/* Плагин темы HeroUI для Tailwind 4. В четвёртой версии плагины подключаются
   из CSS (`@plugin`), а не из `tailwind.config.js` — конфига-файла больше нет.

   Здесь же настраивается фирменная тема, если понадобится: `heroui({ themes:
   … })`. Пока набор берём в его исходном виде, чтобы было видно, что именно
   даёт библиотека. */
import { heroui } from '@heroui/theme';

export default heroui();
