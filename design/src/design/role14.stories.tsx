/* Дизайн · 14 · Спортсмен — варианты экранов.

   Раздел «Дизайн» отвечает на вопрос «каким сделать экран», раздел «Макеты» —
   «как выглядит маршрут роли целиком». Пока по экрану не выбран вариант, в
   «Макетах» живёт прежний макет; выбрали — он туда и переезжает, а вариант
   остаётся здесь историей решения. */

import { Option, OptionBoard } from './opt';
import { HomeA, HomeB, HomeV } from './role14home';

export default {
  title: 'Дизайн/14 · Спортсмен',
  parameters: { layout: 'fullscreen' },
};

const A_PROS = [
  'Во время турнира отвечает с двух метров: стол, соперник, время.',
  'Одно действие — «Открыть матч»; спорить за внимание нечему.',
  'Рейтинг остаётся на экране, но строкой — он справка, а не заголовок.',
];
const A_CONS = [
  'Вне турнира верх экрана пустует — нужен второй, равноценно нарисованный вид.',
  'Личность спортсмена (фото, разряд, взнос) уходит в шапку и профиль.',
];

const B_PROS = [
  'Экран одинаково осмыслен и в турнир, и между турнирами.',
  'Хроника показывает и прошлое, и будущее: вызов, заявка, старт, пересчёт.',
  'Взнос виден постоянно — а он блокирует заявки (⚠ 6.1).',
];
const B_CONS = [
  'Вызов на стол — строка в ленте, а не крик: во время игры его надо искать.',
  'Левый столбец занимает треть ширины ради данных, которые меняются раз в месяц.',
];

const V_PROS = [
  'Один макет на сайт и приложение — роль единственная, у кого есть и то, и другое (TZ §10).',
  'Ничего не ломается на узком экране: колонка просто сужается.',
  'Заливка ровно одна — «вас вызвали», и глаз находит её без поиска.',
];
const V_CONS = [
  'На широком мониторе половина экрана — воздух.',
  'Плотность ниже: то, что у А и Б помещается в один экран, здесь требует прокрутки.',
];

export const Home = {
  name: 'Э14.1 · Главная — 3 варианта',
  render: () => (
    <OptionBoard
      code="Э14.1"
      title="Главная спортсмена"
      tag="Содержание у всех трёх одно и задано flows/14-sportsmen.md: рейтинг с местом и дельтой, ближайший турнир с состоянием заявки, «сейчас играю» и две новости федерации. Отличается решение — что на экране главное. Оболочка роли одна и та же: сравниваем экран, а не хром."
    >
      <Option
        letter="А"
        name="Вызов на стол"
        idea="главное — играешь ли ты сейчас и куда идти"
        pros={A_PROS}
        cons={A_CONS}
      >
        <HomeA />
      </Option>

      <Option
        letter="Б"
        name="Карточка спортсмена"
        idea="слева кто я, справа что дальше — хроникой"
        pros={B_PROS}
        cons={B_CONS}
        adds={[
          'Кривая рейтинга на главной (данные те же, что в аналитике Э14.6).',
          'Доля побед и состояние взноса вынесены на главную из профиля (Э14.7).',
        ]}
      >
        <HomeB />
      </Option>

      <Option
        letter="В"
        name="Лента"
        idea="одна колонка ширины чтения — тот же экран ложится в приложение"
        pros={V_PROS}
        cons={V_CONS}
        adds={['Обложки у новостей на главной — как в ленте новостей (Э14.13).']}
      >
        <HomeV />
      </Option>
    </OptionBoard>
  ),
};

export const OptionA = { name: 'Вариант А · Вызов на стол', render: () => <HomeA /> };
export const OptionB = { name: 'Вариант Б · Карточка спортсмена', render: () => <HomeB /> };
export const OptionV = { name: 'Вариант В · Лента', render: () => <HomeV /> };
