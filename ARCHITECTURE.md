# Системный дизайн

Из чего состоит система.

- Пользовательские сценарии — [USERFLOW.md](USERFLOW.md)
- Требования — [TZ.md](TZ.md) · Дизайн — [DESIGN.md](DESIGN.md)

![Архитектура](diagrams/out/architecture.svg)

---

## 1. Клиенты и их форм-факторы

Форм-фактор здесь — не деталь, а требование. Он вытекает из того, **где физически
находится человек**, и определяет, что вообще можно нарисовать на экране.

| Роль | Устройство | Почему так |
|---|---|---|
| Администратор системы | **десктоп** | аккаунты, организации, настройки, публикация контента |
| Главный судья | **десктоп** | создание и ведение турнира, конструктор сетки, назначения, модерация заявок |
| Судья стола | **адаптивный сайт, обычно планшет** | ведёт протокол за столом; нужен крупный счёт и кнопки регламента; работает и с телефона |
| Тренер, представитель | **десктоп** | формирует список игроков, подаёт заявку |
| Игрок | **сайт и приложение iOS/Android** | в зале между матчами и дома; смотрит свой матч и аналитику |
| Гость | любое, веб | только чтение |

---

## 2. Архитектура

```mermaid
flowchart LR
    subgraph client ["Клиенты"]
        judgeTablet["Устройство судьи: адаптивный сайт, офлайн-очередь"]
        playerPhone["Игрок: сайт и приложение iOS/Android"]
        adminDesktop["Десктоп: администратор системы, главный судья, тренер"]
        guestWeb["Гостевой просмотр"]
    end
    subgraph gateway ["API"]
        apiGateway["HTTPS, JWT в куках"]
    end
    subgraph service ["Сервисы"]
        authService["Аутентификация"]
        rbacService["Авторизация: роль, область, право"]
        tournamentService["Турниры и столы"]
        applicationService["Заявки"]
        scoreService["Счёт: розыгрыши и партии"]
        refereeService["Регламент: подача и смена сторон"]
        analyticsService["Аналитика игрока: бесплатная и платная"]
        liveService["Счёт в реальном времени по очкам: много столов"]
        broadcastService["Трансляция на YouTube: счёт поверх видео"]
        notifyService["Уведомления"]
        engine["Движок турниров и конструктор сеток: ПЕРЕИСПОЛЬЗУЕТСЯ"]
        ratingService["Расчёт рейтинга: заложенная методика, ELO"]
    end
    subgraph datastore ["Данные"]
        postgres["PostgreSQL"]
        auditLog["Аудит счёта: неизменяемый"]
    end

    judgeTablet <-->|"HTTPS, live"| apiGateway
    playerPhone <-->|"HTTPS, live"| apiGateway
    adminDesktop <-->|"HTTPS, live"| apiGateway
    guestWeb -->|"Публичное чтение"| apiGateway

    apiGateway -->|"Вход"| authService
    apiGateway -->|"Проверка прав"| rbacService
    apiGateway -->|"Турниры"| tournamentService
    apiGateway -->|"Заявки"| applicationService
    apiGateway -->|"Счёт"| scoreService
    apiGateway -->|"Регламент"| refereeService
    apiGateway -->|"Аналитика"| analyticsService
    apiGateway -->|"Подписка"| liveService

    scoreService -->|"Партия сыграна"| engine
    scoreService -->|"Пишет каждую правку"| auditLog
    scoreService -->|"Событие счёта"| liveService
    scoreService -->|"Счёт для наложения"| broadcastService
    refereeService -->|"Пишет события матча"| auditLog
    tournamentService -->|"Генерация и конструктор сетки"| engine
    tournamentService -->|"Завершение турнира — пересчёт"| ratingService
    engine -->|"Итоги матчей"| ratingService
    engine -->|"Матч готов"| notifyService

    authService -->|"Пользователи"| postgres
    rbacService -->|"Роли и области"| postgres
    applicationService -->|"Заявки"| postgres
    refereeService -->|"События матча"| postgres
    analyticsService -->|"История и винрейт"| postgres
    liveService -->|"Читает состояние"| postgres
    notifyService -->|"Уведомления"| postgres
    engine -->|"Матчи, партии, сетка, шаблоны"| postgres
    ratingService -->|"Рейтинги в историю"| postgres
```

### Принятые решения

**Счёт и регламент — разные сервисы.** Ввод очка и смена сторон выглядят как
«кнопки на одном экране», но это разные вещи: первое меняет результат матча и
рейтинг, второе — процедурное событие. Смешивать их в одном сервисе значит
смешивать и правила отката.

**Движок — отдельный узел, хотя физически это библиотека.** Показан отдельно
потому, что он **переиспользуется** из существующей системы, и его границу важно
видеть: всё, что внутри, не переписывается. Даёт стандартные форматы **и
конструктор нестандартных сеток** (см. [TZ.md](TZ.md) §5).

**Рейтинг — по заложенной методике.** Методика единая для всех турниров и
определена в системе (ELO с фиксированными коэффициентами, см. [TZ.md](TZ.md)
§7). Пересчёт запускается по завершении турнира и не задваивается при повторе.
Считается рейтинг игроков и судей.

**Роли:** сверху — **Администратор системы** (пользователи, организации,
настройки, контент); **Главный судья** руководит конкретным турниром (создаёт,
собирает сетку, вызывает пары, завершает); ниже — судья стола, тренер/
представитель, игрок, гость.

**Счёт в реальном времени — отдельный компонент, транспорт не выбран.**
Обновление счёта у зрителей — архитектурное решение (частый опрос против
постоянного соединения), и его надо принять осознанно, а не унаследовать.
Работает на многих столах одновременно.

**Трансляция на YouTube — отдельный сервис.** Счёт накладывается на видео со
столов автоматически из ввода судьи (см. [TZ.md](TZ.md) §6.3), без оператора,
и встраивается на страницу турнира. Отделён от счёта, потому что это внешняя
интеграция со своим темпом и режимом сбоев.

**Мобильное приложение игрока — отдельный клиент, общий backend.** Приложение
под Android и iOS работает на тех же сервисах и данных, что и сайт; игрок
пользуется и сайтом, и приложением. Расширенная аналитика платная — единственная
точка монетизации (см. [TZ.md](TZ.md) §11).

**Аутентификация и авторизация разведены.** RBAC с шестью ролями и наследованием —
ядро этого ТЗ, а не деталь входа по паролю.

---

## 3. Офлайн-очередь на устройстве судьи

Единственное место, где клиент — не тонкий. Сеть в спортзале ненадёжна, а потеря
счёта недопустима.

![Офлайн-очередь](diagrams/out/flow-offline.svg)

```mermaid
sequenceDiagram
    title Офлайн-очередь: счёт не теряется при обрыве
    participant Судья
    participant Устройство
    participant Очередь
    participant API
    participant Аудит

    Судья->>Устройство: Вводит очко
    Устройство->>Очередь: Кладёт событие локально
    Устройство-->>Судья: Счёт на экране сразу
    Устройство->>API: Пытается отправить
    API-->>Устройство: Сеть недоступна
    Устройство-->>Судья: Статус: сохранено локально, N в очереди

    Судья->>Устройство: Продолжает вести счёт
    Устройство->>Очередь: Копит события по порядку

    Устройство->>API: Сеть вернулась — отправляет очередь по порядку
    API->>Аудит: Пишет каждое событие с временем на устройстве
    API-->>Устройство: Подтверждение
    Устройство-->>Судья: Статус: синхронизация активна
```

**Ключевое правило: очередь упорядочена и идемпотентна.** Каждое событие несёт
время, снятое **на устройстве**, а не на сервере — иначе после обрыва порядок
розыгрышей восстановится неверно. Повторная отправка того же события не должна
менять счёт дважды.

---

## 4. Доменная модель

![Доменная модель](diagrams/out/domain.svg)

Ключевые сущности:

- **`ROLE`** — не флаг на пользователе, а связка «пользователь → роль → область»
  (система / турнир / клуб / стол). Один человек может быть тренером в клубе А,
  судьёй стола на турнире Б и игроком на турнире В одновременно.
- **`ENTRY`** — **сторона матча**: одиночка (один игрок) или пара (двое). Матч
  связывает две стороны, а не двух игроков, поэтому **парный разряд** укладывается
  в ту же модель без переделки.
- **`GAME`** — партия. Матч не хранит счёт напрямую: `3:1` — это производная от
  партий. Партии нужны и судье (таблица), и игроку (история матчей).
- **`RALLY`** — розыгрыш, одно очко. Из него строится лента последних розыгрышей
  и работает точечная отмена.
- **`MATCH_EVENT`** — процедурные события: смена сторон и смена подачи.
  Отдельно от счёта, потому что не влияют на результат.
- **`SCORE_LOG`** — неизменяемый аудит. Любая правка счёта, включая
  переопределение главным судьёй, восстановима по автору и времени.
- **`RATING_POINT`** — точка рейтинга **игрока или судьи** (`kind`). Значения
  считает внешняя система (см. раздел 2), мы храним историю для графиков.

```mermaid
erDiagram
    USER ||--o{ ROLE : "имеет"
    TOURNAMENT ||--o{ ROLE : "область роли"
    CLUB ||--o{ ROLE : "область роли"
    TOURNAMENT_TABLE ||--o{ ROLE : "область роли"

    CLUB |o--o{ APPLICATION : "подаёт — если от тренера"
    USER ||--o{ APPLICATION : "автор заявки"
    TOURNAMENT ||--o{ APPLICATION : "получает"
    APPLICATION ||--|{ APPLICATION_ITEM : "содержит игроков"
    USER ||--o{ APPLICATION_ITEM : "заявленный игрок"
    APPLICATION_ITEM ||--o| ENTRY : "принята — создаёт сторону"

    TOURNAMENT ||--o{ TOURNAMENT_TABLE : "имеет"
    TOURNAMENT ||--o{ ENTRY : "составы и посев"
    TOURNAMENT ||--o{ MATCH : "содержит"

    USER ||--o{ ENTRY : "игрок стороны — 1 или 2"
    ENTRY ||--o{ MATCH : "сторона матча"
    TOURNAMENT_TABLE ||--o{ MATCH : "проводит"

    MATCH ||--|{ GAME : "состоит из партий"
    GAME ||--o{ RALLY : "розыгрыши"
    MATCH ||--o{ MATCH_EVENT : "смена сторон и подачи"
    MATCH ||--o{ SCORE_LOG : "правки счёта"
    USER ||--o{ SCORE_LOG : "автор правки"

    USER ||--o{ RATING_POINT : "рейтинг игрока или судьи"
    TOURNAMENT ||--o{ RATING_POINT : "изменение по итогам"
    TOURNAMENT }o--o| BRACKET_TEMPLATE : "собрана по шаблону"
    USER ||--o{ BRACKET_TEMPLATE : "автор шаблона"

    USER {
        uuid id PK
        string name
        string phone UK
        string city
        string photoUrl
        uuid clubId FK "необязателен"
        int rating "текущий"
    }
    ROLE {
        uuid id PK
        uuid userId FK
        string kind "администратор системы | главный судья | тренер | судья стола | игрок"
        string scope "система | турнир | клуб | стол"
        uuid scopeId "id области"
    }
    CLUB {
        uuid id PK
        string name
        string city
    }
    TOURNAMENT {
        uuid id PK
        string name
        string format "олимпийка | двойное выбывание | группы+плей-офф | круговая | швейцарская | все места | командный"
        string discipline "одиночный | парный | командный"
        string category "мужчины | женщины | микст"
        string status "черновик | заявки | идёт | завершён"
        int gamesToWin "партий до победы — обычно 3 из 5"
        datetime startsAt
    }
    TOURNAMENT_TABLE {
        uuid id PK
        uuid tournamentId FK
        int number
        uuid refereeId FK "судья стола — без него стол не принимает матчи"
    }
    APPLICATION {
        uuid id PK
        uuid tournamentId FK
        uuid clubId FK "пусто, если игрок заявился сам"
        uuid submittedBy FK "тренер или сам игрок"
        string source "клуб | игрок сам"
        string status "подана | принята | отклонена | отозвана"
        string rejectReason
    }
    APPLICATION_ITEM {
        uuid id PK
        uuid applicationId FK
        uuid userId FK "заявленный игрок"
    }
    ENTRY {
        uuid id PK
        uuid tournamentId FK
        uuid player1Id FK
        uuid player2Id FK "пусто в одиночке"
        int seed
        int place
    }
    MATCH {
        uuid id PK
        uuid tournamentId FK
        uuid tableId FK
        string round "1/8 | 1/4 | 1/2 | финал"
        uuid sideAId FK "ENTRY"
        uuid sideBId FK "ENTRY"
        uuid servingPlayerId FK "кто подаёт сейчас — игрок, даже в паре"
        string status "ожидает | идёт | завершён"
        uuid winnerSideId FK "ENTRY-победитель"
        boolean isWalkover "техническая победа"
    }
    GAME {
        uuid id PK
        uuid matchId FK
        int number "номер партии, 1..5"
        int score1
        int score2
        string status "ожидает | идёт | завершена"
    }
    RALLY {
        uuid id PK
        uuid gameId FK
        int number "порядок в партии"
        uuid wonBy FK "кто выиграл очко"
        datetime atDevice "время НА УСТРОЙСТВЕ — для порядка после офлайна"
        uuid enteredBy FK "судья стола"
    }
    MATCH_EVENT {
        uuid id PK
        uuid matchId FK
        string kind "смена сторон | смена подачи"
        uuid recordedBy FK "судья стола"
        datetime at
    }
    SCORE_LOG {
        uuid id PK
        uuid matchId FK
        uuid authorId FK
        datetime at
        string before
        string after
        string reason "правка судьи | переопределение главным судьёй"
    }
    RATING_POINT {
        uuid id PK
        uuid userId FK
        string kind "игрок | судья"
        uuid tournamentId FK
        int value "рейтинг после турнира — по заложенной методике"
        int change "дельта, например +24"
        datetime at
    }
    BRACKET_TEMPLATE {
        uuid id PK
        string name
        string type "SE | DE | RR | groups"
        json configJson "конфигурация сетки"
        boolean isCustom "из конструктора"
        uuid createdBy FK
    }
```

### Ключевые решения модели

- **`GAME` — отдельная сущность.** Матч не хранит счёт напрямую: `3:1` — это
  производная от партий. Партии нужны и судье (таблица), и игроку (история).
- **`RALLY`** нужен для ленты последних розыгрышей и точечной отмены; он же
  покадровый счёт для live-трансляции и аналитики.
- **`MATCH_EVENT`** — смена сторон и подачи не влияют на счёт, но фиксируются.
- **`ENTRY` — ключ к парному разряду.** Матч связывает две стороны, каждая —
  один игрок (одиночка) или двое (пара). Посев, заявка и рейтинг работают поверх
  стороны, поэтому парный разряд лёг в модель без переделки матча.
- **Рейтинг по заложенной методике.** Методика единая и определена в системе
  (ELO, фиксированные коэффициенты). `RATING_POINT` — история значений, `kind`
  разделяет рейтинг игроков и судей.
- **`BRACKET_TEMPLATE`** — конструктор сеток сохраняет шаблоны в библиотеку.

**`RALLY.atDevice` — не украшение.** Это время, снятое на устройстве судьи, а не на
сервере. После обрыва сети очередь событий уходит пачкой, и серверное время
у всех будет почти одинаковым — порядок розыгрышей восстановится неверно.
