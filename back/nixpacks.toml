[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput",
]

[start]
cmd = "python manage.py migrate --noinput && gunicorn tt_back.wsgi:application --bind 0.0.0.0:$PORT"
